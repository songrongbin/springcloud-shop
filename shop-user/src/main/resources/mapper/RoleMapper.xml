<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bins.springcloud.shop.user.mapper.RoleMapper">
    
    <resultMap id="BaseResultMap" type="com.bins.springcloud.shop.user.entity.RoleEntity" >
   		<id column="id" property="id" jdbcType="BIGINT"/>
           <result column="role_code" property="roleCode" jdbcType="VARCHAR"/>
           <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
           <result column="operator_id" property="operatorId" jdbcType="BIGINT"/>
           <result column="sort" property="sort" jdbcType="INTEGER"/>
           <result column="is_del" property="isDel" jdbcType="INTEGER"/>
           <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
           <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>    
    </resultMap>
    
    <select id="findRoleList" resultMap="BaseResultMap">
        select * from t_role where is_del = 0
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="Long">
        select * from t_role where id = #{id}
    </select>
    
    <insert id="insertRole" parameterType="com.bins.springcloud.shop.user.dto.RoleDto">
    	INSERT INTO `t_role` (role_code, role_name, operator_id, is_del) VALUES 
    	(#{roleCode}, #{roleName}, #{operatorId}, #{isDel})
    </insert>
    
    <update id="updateRole" parameterType="com.bins.springcloud.shop.user.dto.RoleDto">
    	update `t_role` set role_code = #{roleCode}, role_name = #{roleName},
    	operator_id = #{operatorId}, sort = #{sort} where id = #{id}
    </update>
    
    <update id="deleteRole" parameterType="com.bins.springcloud.shop.user.dto.RoleDto">
    	update `t_role` set is_del = 1 where id = #{id}
    </update>
    
   <insert id="insertRolePermission" parameterType="map">
        INSERT INTO `t_role_permission` (role_id, permission_id) VALUES
            (#{roleId}, #{permissionId})
    </insert>

</mapper>